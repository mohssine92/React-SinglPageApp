import { createContext } from 'react';

export const AuthContext = createContext(); // posteriormente veamos un ejcmplo donde crearteContext( reciba arg )

// esto es todo lo que necesito para crar un context para un Reducer funcion 
